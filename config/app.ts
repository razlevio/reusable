import { AppConfig } from "@/types"

export const appConfig: AppConfig = {
  name: "next-stack-boilerplate",
  description: "my next-stack structure boilerplate",
  url: "https://github.com/razlevio",
}
